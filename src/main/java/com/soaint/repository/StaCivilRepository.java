package com.soaint.repository;

import com.soaint.entity.StaCivil;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StaCivilRepository extends JpaRepository<StaCivil, Long> {
}
